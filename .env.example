# Copy to .env and fill in real values (never commit .env)

# --- Roboflow ---
ROBOFLOW_API_KEY=
ROBOFLOW_WORKSPACE=
ROBOFLOW_PROJECT=
ROBOFLOW_VERSION=1

# --- Hugging Face ---
HF_TOKEN=
HF_REPO_ID=srnortw/mask2former-lane-seg
CONF_THRESH=0.5

# --- MongoDB Atlas (project: mask2former-mlops) ---
# Cluster: mask2former-cluster (EU_CENTRAL_1)
# Setup: ./scripts/setup_atlas_mongo.sh
MONGO_URI=mongodb+srv://mask2former_api:PASSWORD@mask2former-cluster.ceg04zs.mongodb.net/mask2former?retryWrites=true&w=majority
MONGO_DB_NAME=mask2former
MONGO_COLLECTION_PREDICTIONS=predictions
MONGO_COLLECTION_DRIFT=drift_reports
ATLAS_PROJECT_ID=6a21b995cbf3f23e5981be8f
ATLAS_CLUSTER=mask2former-cluster

# --- MLflow (DagsHub) ---
MLFLOW_TRACKING_URI=
MLFLOW_TRACKING_USERNAME=
MLFLOW_TRACKING_PASSWORD=
MLFLOW_EXPERIMENT_NAME=mask2former-swin

# --- GitHub (optional, for push from Colab) ---
GITHUB_TOKEN=
