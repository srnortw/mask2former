[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [".venv", "notebooks", "data", "reports", "ros2_ws"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
